/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * user guide available at https://docs.gradle.org/5.0/userguide/java_library_plugin.html
 */
class youtube implements Plugin<Project>
{
	
	void apply (Project project)
	{
		project.task("Play")
		{
			doLast{
				println "Playing yputube channel...."
			}
		}
	}
}
apply plugin : 'java-library'
apply plugin : youtube
apply plugin : 'application'

mainClassName = 'Demo.helloGradle'
mainClassName = 'Demo.Add'


repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
	mavenCentral()
}

dependencies {
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api 'org.apache.commons:commons-math3:3.6.1'

    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    implementation 'com.google.guava:guava:26.0-jre'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
}
task myTask{
	
	   group "MycustomeTask"
		doLast{
			
		}
}

task myTask2(dependsOn:'myTask'){
		group "MycustomeTask"
		doLast{
			println "How are you"
		}
}

task copyFile (type : Copy) {
	group "MycustomeTask"
	from '/home/axelor/my-work-space/Myapp/new.txt'
	into '/home/axelor/my-work-space'
}

task myTask3(dependsOn:'myTask'){
	group "MycustomeTask"
	doLast{
		println "How are you"
	}
}

